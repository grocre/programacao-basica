// falando sobre git e github -> USEM -> falar do uso das ferramentas na Byte

//começar falando sobre cometários

// falar sobre linguagens que são fortemente tipadas como o C

/*

    int n1 = 2;
    char[] nome = "Gabriel";
    float altura = 1.80;

*/

// o uso de ponto e virgula é opcional
// as aspas podem ser duplas ou simples

var preco = 15 // falar sobre alocação de memória

// vamos fazer um teste
console.log(preco, typeof(preco)) // inteiro
// assim como no pyhton, a virgula dá um espaco entre as coisas que serão impressas no console

var preco = 15.5
console.log(preco, typeof(preco)) // ponto flutuante

//nas duas situações o preco é dado por um número, ou seja, o js aloca um espaço predefinido na memória que de adequa as duas situações

preco = "15";
console.log(preco, typeof(preco))
//preco

preco = true
console.log(preco, typeof(preco))

//falar que são os principais tipos primitivos da linguagem

// falar sobre o TypeScript que vem pra resolver alguns problemas que a falta de tipagem pode criar

